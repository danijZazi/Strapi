{
  "routes": [
    {
      "method": "GET",
      "path": "/expenses",
      "handler": "expenses.getExpenses",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/expenses/:expenseId",
      "handler": "expenses.getExpense",
      "config": {
        "policies": []
      }
    }
    
  ]
}
